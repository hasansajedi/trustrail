# API reference

The primary public imports are available directly from `aiRail`:

```python
from aiRail import Guard, GuardAction, GuardContext, GuardStage
```

Start with [`Guard`](guard.md), then use the model and enum reference when you
need to construct contextual checks or inspect structured results. Advanced
modules expose streaming scanners, agent sessions, audit sinks, and integration
adapters.

## Public package

::: aiRail
    options:
      members: true
      show_root_heading: false
      show_root_toc_entry: false
